# BreakingTrades Dashboard — OpenSpec Index

> Last updated: 2026-05-31 (autoresearch durable run ledger shipped)
> Status: **Production (v1)** + **Staging (v2 SPA)** 
> v1 live at: https://breakingtrades.github.io/breakingtrades-dashboard/
> v2 staging at: https://brave-glacier-07c70460f.2.azurestaticapps.net/root index.html
> Azure SWA auto-deploys from GitHub on every push.

## Architecture

### v1 (Current Production) — Multi-Page
6 standalone HTML files, each with inline CSS/JS. Full page reload on navigation.

### v2 (Staging) — Single Page Application
Vanilla JS SPA in `root ` subdirectory. Hash router, persistent nav + ticker tape, shared component library. 33 files, ~6,800 lines. Zero framework, zero build step.

| Layer | Files | Description |
|-------|-------|-------------|
| **Shell** | `index.html`, `shell.js`, `router.js`, `app.js`, `preferences.js` | Single HTML shell, hash router, localStorage preferences |
| **CSS** | `variables.css`, `reset.css`, `shell.css`, `components.css` + 7 page CSS | Extracted from v1 inline styles, single `:root` token source |
| **Components** | `fear-greed.js`, `vix-regime.js`, `detail-modal.js`, `collapsible.js` | Shared across pages, deduplicated from v1's 3 modal implementations |
| **Pages** | `market.js`, `signals.js`, `watchlist.js`, `expected-moves.js`, `events.js`, `autoresearch.js` | Lazy-loaded by router, register on `BT.pages` namespace |
| **Lib** | `bt-prices.js`, `market-status.js`, `ticker-search.js`, `ticker-tape.js`, `sector-rotation.js` | Copied from v1, data paths adjusted to `../data/` |
| **Icons** | Lucide Icons via CDN (`unpkg.com/lucide@latest`) | Replaced all emoji + inline SVGs with consistent monoline SVG icons |
| **Hosting** | Azure Static Web Apps (Free tier, eastus2) | Auto-deploy via GitHub Actions, auth-ready, PR preview environments |

## Production Pages

| Page | v1 File | v2 Route | Description |
|------|---------|----------|-------------|
| **Market** (landing) | `market.html` | `#market` | Sector heatmap, RRG, Fear & Greed gauge, VIX regime, pair ratios, sector rankings, market breadth |
| **Signals** | `signals.html` | `#signals` | Trade setup cards with lifecycle tracking, detail modals + TradingView charts, right panel (F&G, regime, briefing) |
| **Watchlist** | `watchlist.html` | `#watchlist` | 74-symbol tracker, table view, detail modals, EM banner, hash routing (`#watchlist/SPY`) |
| **Expected Moves** | `expected-moves.html` | `#expected-moves` | Options EM risk heatmap, position bars, alert tags, staleness guard, filter/tier tabs, inline detail modal |
| **Events** | `events.html` | `#events` | Calendar page, live countdowns, category filters, mini strip on signals |
| **Autoresearch** | `autoresearch.html` | `#airesearcher` | AI Researcher — strategy results (performance by regime, research findings, rules in play) + regime intelligence (15-signal scoring, playbook, market internals, commodity chain, transition signals) |

## Production Data Pipeline

| Component | Schedule | Description |
|-----------|----------|-------------|
| **Canonical Prices** | 4× daily (9:30/12/3/4:05 ET) + EOD | `update-prices.py` → `data/prices.json` — single source of truth for all ticker prices. `bt-prices.js` shared module consumed by all pages. |
| **Futures Strip** | 4× daily (9:30/12/3/4:05 ET) + EOD | `update_futures.py` → `data/futures.json` — 14 instruments. **No longer rendered on any page** — replaced by TradingView real-time ticker tape (`js/ticker-tape-tv.js`). JSON still generated for pipeline/API consumers. `macro-context.js` also removed from all pages (was the second static strip showing SPY/QQQ/VIX/F&G with SMA20 tags). |
| **Fear & Greed** | ~Hourly cron | `update-fear-greed.py` → `data/fear-greed.json` |
| **VIX** | 4× daily (9:30/12/3/4:05 ET) | `update-vix.py` → `data/vix.json` |
| **Sector Rotation** | On demand / EOD | `export-sector-rotation.py` → `data/sector-rotation.json` + `data/sector-risk.json` |
| **Market Breadth** | EOD | `update-breadth.py` → `data/breadth.json` — 11 GICS sectors + 5 indices, multi-timeframe (20/50/100/200d SMA) |
| **Expected Moves** | EOD 4:20 PM ET (GH Action) + local fallback | `update-expected-moves.py` — IB Gateway first, yfinance fallback (Polygon removed). `--source auto/ib/yfinance`. `BT_EM_SOURCE` env var for CI/containers. → `data/expected-moves.json` |
| **EM Quarterly History** | Post-quarter-end (EOD Step 7) + manual | `capture-quarterly-em.py` — IB historical IV at each quarter-end date for 78 tickers. Auto-runs first 3 trading days after quarter end if IB Gateway available. → `data/em-quarterly-history.json` |
| **EOD Pipeline** | GH Actions 4:20 PM ET + Mac cron 4:25 PM ET Mon-Fri | `eod-update.sh` — single runner: F&G + VIX + sector rotation + EM + yfinance fallback + autoresearch summary. **Last trading day of week** runs all EM tiers (weekly/monthly/quarterly). **Staleness guard:** if weekly EM anchor >7 days old, forces `--tier all` regardless of day (self-heals after missed Friday push). GH Actions pushes at 4:20; Mac cron runs at 4:25 (5-min offset prevents race condition / rebase conflicts that caused anchor staleness). |
| **Autoresearch Summary** | EOD (step 5b) | `autoresearch/summarize.py` → `data/autoresearch-summary.json` — regenerates dashboard AI Researcher display from latest optimizer results. Fast (<1s), no network. Added 2026-04-28. |
| **Daily Briefing** | Every 30 min | `generate-briefing.py` → `data/briefing.json` |
| **Dashboard Export** | On demand | `export-dashboard-data.py` — bridge from parent data CSVs → dashboard JSON |
| **Trump Monitor** | On demand | `trump-monitor.py` — Google News RSS → LLM classify → `events.jsonl` |
| **Economic Calendar** | On demand / pre-EOD | `fetch-economic-calendar.py` — Investing.com → US 3-star events only → `data/economic-calendar.json`. Frontend `events.js` merges into Live Countdowns. Top 3 upcoming within 14 days. No API key. |

## Shipped Changes (Chronological)

| Change | Date | Commits | Description |
|--------|------|---------|-------------|
| [V3 Tape Toggle Fix](changes/v3-sidebar-dashboard/) | 2026-06-14 | (this change) | Restore the OFF / SNAPSHOT / TAPE cycle in V3 chrome. **Bug:** clicking from snapshot back to tape silently no-op'd because `tickerTape` exposed only `show`/`hide` while `shell.js` was calling `mount`/`destroy`. **Fix (3 files):** (1) `js/lib/ticker-tape.js` — added symmetric `mount()` and `destroy()`; destroy clears `_injected`+`_wrap` so a later `mount()` rebuilds the widget; auto-inject on `nav:ready` is suppressed in v3 mode (shell.js owns lifecycle there). Tape now mounts inside `main.v3-main` so it's visible inside the V3 grid viewport. (2) `js/lib/snapshot-strip.js` — stopped reaching into `.tradingview-widget-container` from `mount()`; that orphaned ticker-tape's wrapper. snapshot-strip now also mounts inside `main.v3-main`. (3) `js/shell.js` — attach `v3:tape-state-change` listener BEFORE `V3Sidebar.mount()` so the initial `setTapeState()` dispatch (which restores the saved state) is actually received. (4) `tests/tape-toggle.test.js` — 6 regression assertions on API surface, destroy semantics, and listener-before-mount ordering. |
| [Watchlist Engine](changes/archive/2026-03-17-watchlist-engine/) | 2026-03-17 | — | Initial watchlist data + export pipeline |
| [Dashboard Frontend](changes/archive/2026-03-17-dashboard-frontend/) | 2026-03-17 | — | Initial 3-page dashboard build |
| [Tom Agent](changes/archive/2026-03-17-tom-agent/) | 2026-03-17 | — | Tom persona files + rules |
| [CI Data Pipeline](changes/ci-data-pipeline/proposal.md) | 2026-03-18 | `a03bc1d` | Self-sustained yfinance fallback + GitHub Models briefing |
| [Market Status Indicator](changes/market-status-indicator/proposal.md) | 2026-03-18 | `44f01b5`, `501b559` | Real-time open/closed/pre/after with holidays. **Updated 2026-04-02:** all times localized to user's browser timezone (open/close/clock), no hardcoded ET. |
| [Sector Rotation Chart](changes/sector-rotation-chart/OPENSPEC.md) | 2026-03-18 | `44fa968` | RRG chart + sector risk badges (4 phases) |
| [Logo & Branding](changes/logo-branding/proposal.md) | 2026-03-19 | — | Inline SVG logo mark in nav bar |
| [Ticker Search](changes/ticker-search/proposal.md) | 2026-03-19 | — | Global search bar with tracked (enriched) vs external (TradingView) routing |
| [Futures Strip](changes/futures-strip/proposal.md) | 2026-03-19 | — | Pre-market futures/macro data strip (14 instruments, 5 groups). **Fully superseded by TradingView real-time ticker tape on all pages (2026-03-31).** `futures-strip.js` and `macro-context.js` no longer loaded. |
| [Shared Nav Component](changes/shared-nav-component/) | 2026-03-20 | — | Nav bar, ticker search, market status, timezone picker — shared across all pages |
| [Expected Moves](changes/expected-moves/proposal.md) | 2026-03-20 | `c9e8957`..`3b8b053` | EM page with risk model, watchlist banner, staleness guard + staleness tests |
| [Watchlist Page](changes/watchlist-page/) | 2026-03-20 | — | Widget + table views, TradingView embed, detail modals |
| [Market Page](changes/market-page/) | 2026-03-20 | — | Heatmap, RRG, F&G, VIX, pairs, sector rankings |
| [Event Calendar](changes/event-calendar/proposal.md) | 2026-03-22 | `d2f8d8c`..`d1f34ec` | `events.html` full calendar page + `events.js` renderer + mini strip on index + `events.jsonl` store + `bt-event` CLI + `trump-monitor.py` + `extract-events.py` |
| [IB Gateway TOTP Automation](changes/expected-moves/) | 2026-03-23 | — | Fully unattended IB Gateway login: Keychain TOTP secret + `totp-helper.sh` + `start-gateway.sh`; live account `idansh235`, port 4002 |
| [EM Watchlist Filter + Top 10 S&P + Bias](changes/expected-moves/) | 2026-03-23 | `16eb624` | Filter tabs (All / Top 10 S&P / Watchlist) + Bias column (▲ BULL / ▼ BEAR + ↑↓200 SMA) |
| [Market Data Refresh](changes/expected-moves/) | 2026-03-23 | `237df55` | EM 55/72 tickers via IB live, F&G 16.1 (Extreme Fear), sector rotation + watchlist refresh |
| [generate-brief.py Cron](changes/ci-data-pipeline/) | 2026-03-23 | — | `*/30 * * * *` cron added; stale `hourly-brief.sh` cron removed |
| [.nojekyll Fix](changes/ci-data-pipeline/) | 2026-03-24 | `18ed114` | Added `.nojekyll` — bypasses Jekyll build, fixes Pages deploy failures from `node_modules` Liquid syntax |
| [Signals Page F&G Fix](changes/ci-data-pipeline/) | 2026-03-24 | `89eff9d` | Signals page F&G now fetches from `data/fear-greed.json` (was hardcoded) |
| [EM Filter Tab Style Fix](changes/expected-moves/) | 2026-03-24 | `012c36a` | Filter tabs match `tier-tab` style; both tab rows on one line; removed emoji |
| [EM Indices Filter](changes/expected-moves/) | 2026-03-24 | `ead37d4` | Indices filter tab (SPY, QQQ, IWM, DIA, TLT, HYG, LQD, GLD, USO, UNG, IBIT) |
| [EM Detail Modal](changes/expected-moves/) | 2026-03-24 | `5cef627` | Inline detail modal on EM page — TradingView chart + all 4 EM tier cards; works for all tickers incl. USO/GLD/IBIT; Escape to close |
| [EM Filter Tab Tests](changes/expected-moves/) | 2026-03-24 | `7b81db0` | Filter tab logic tests — Indices/Top10/Watchlist/All + mutual exclusivity |
| [Nav — Market First](changes/dashboard-ui/) | 2026-03-24 | `8b4cd51` | Market page moved to first in navbar; logo + default landing → Market |
| [Root URL → market.html](changes/dashboard-ui/) | 2026-03-24 | `23c7258` | `index.html` replaced with instant redirect to `market.html`; Signals moved to `signals.html` |
| [Market Data Cron](changes/ci-data-pipeline/) | 2026-03-24 | `ba90804` | `update-vix.py` + `update-market-data.sh` — F&G + VIX auto-refresh at 9:30am/12pm/3pm/4:05pm ET Mon–Fri |
| [EOD Update Pipeline](changes/ci-data-pipeline/) | 2026-03-25 | `a95bbc5` | `eod-update.sh` — single EOD runner for F&G + VIX + sector rotation + EM. GitHub Action Mon-Fri 4:20 PM ET, auto-push. Fri runs all EM tiers (weekly/monthly/quarterly), Mon-Thu daily tier only. Local Mac cron fallback. Handles rebase conflicts. |
| [EM Calculator — IB→yfinance](changes/expected-moves/) | 2026-03-25 | `4afa1bb` | Rewrote `update-expected-moves.py` — IB Gateway first, yfinance fallback, Polygon removed. `--source auto/ib/yfinance`. `BT_EM_SOURCE` env var. 57-60 tickers in ~2 min (vs 80+ min Polygon). |
| [EM History Slice Fix](changes/expected-moves/) | 2026-03-25 | `efd6d26` | `history[-52]` → `history[-52:]` — slice not index bug fix |
| [F&G Updated Tooltip](changes/dashboard-ui/) | 2026-03-27 | — | Hover "Fear & Greed Index" title on signals + market pages shows last-updated timestamp (e.g. "Updated: Mar 25, 8:20 PM ET"). `cursor: help` hint. Reads `updated` field from `fear-greed.json`. |
| [Canonical Price Layer](changes/dashboard-ui/) | 2026-03-27 | — | `data/prices.json` + `bt-prices.js` — single source of truth for all ticker prices across all pages. `update-prices.py` fetches 79 tickers from yfinance. All pages (EM, watchlist, signals, market) load `bt-prices.js` and prefer its prices over stale per-file snapshots. EM page drops redundant "Close" column, shows "Price" from canonical source. `macro-context.js` overlays btPrices. `futures.json` added to all pipelines (was orphaned since Mar 19). EOD + intraday pipelines run prices + futures first. Push retry (3×) with stuck-rebase cleanup fixes Mar 26 EOD gap. GH Actions workflow also gets retry. |
| [EM Anchor Fix — previousClose](changes/expected-moves/) | 2026-03-27 | `0aa01c9` | **Bug fix:** EM script used `lastPrice` (live intraday) as anchor → ranges drifted during market hours → all tickers showed ABOVE EM after big moves. Now uses `regularMarketPreviousClose` (yesterday's settled 4 PM close). Fallback chain: `regularMarketPreviousClose` → `previousClose` → `lastPrice`. Option straddle prices still use live data (correct — fresh IV). EM rules fully documented in proposal.md: anchor definition, staleness trap, scaling rules, risk model interpretation, two-source display architecture. |
| [EM Canonical Price Enforcement + Monthly Fallback](changes/expected-moves/) | 2026-03-29 | `d97aee7`, `2f992ed` | **Bug fix:** Both EM scripts (`update-expected-moves-ib.py` + `update-expected-moves.py`) weren't reading from canonical price layer — IB used `reqMktData.close` (stale after hours), yfinance used `regularMarketPreviousClose`. Now both read `prices.json` → `watchlist.json` first (enforcing existing canonical price rule from Mar 27). Also: tickers with no weekly options (17 names like SYK, ADM, XLRE) now fall back to nearest monthly expiry (14-45 DTE) instead of being skipped. Result: 77 tickers with all 4 tiers (was 60). |
| [SPX Native Index Support](changes/expected-moves/) | 2026-03-29 | `0c7cf06` | SPX added to Indices filter + EM data via native IB `Index('SPX', 'CBOE')` contract — real CBOE options chain (706 strikes, 40 expirations). yfinance uses `^SPX` symbol mapping. Both EM scripts handle Index contracts (`secType='IND'`). SPX in DAILY_TICKERS + QUARTERLY_TICKERS. Dashboard: INDICES list, exchange map (`SP:SPX` for TradingView). 78 total tickers. |
| [EM Formula Test Suite](changes/expected-moves/) | 2026-03-29 | `a4eaaa0` | 206 assertions across 10 groups: core formula (`straddle × 0.85`, `√DTE` scaling), tier ordering, index sanity (SPX/SPY/QQQ/IWM/DIA range checks), SPX↔SPY cross-validation, straddle integrity (call+put=straddle, strike near close), formula recomputation from stored data, direct monthly/quarterly straddle override verification, canonical price consistency (0 stale), data completeness (78 tickers, 4 tiers), edge cases (DTE=0, high/low IV). |
| [Search → Detail Routing (System-wide)](changes/shared-nav-component/) | 2026-03-29 | `b7d6264`, `12674ad` | `ticker-search.js` now self-contained: if page has `openDetail()` (signals, watchlist, EM), routes there for all tickers (tracked + external). If page has no modal (market, sector-rotation), creates a lightweight modal on-the-fly with TradingView daily/weekly charts + technical analysis + profile + financials. EM page bridges via `window.openDetail = openEMDetail` and handles external tickers gracefully (TV chart + "Not in watchlist" note). Sort on EM page now 3-state: asc → desc → none. |
| [Market Breadth](changes/market-breadth/proposal.md) | 2026-03-29 | — | Market breadth section on market page: river map (stacked bar, 0-1100), breadth lines (per-sector, 0-100), multi-timeframe table (SPX/NDX/DJI/RUT/VTI × 20/50/100/200d), zone annotations, `update-breadth.py` pipeline (235 stocks via yfinance), added to EOD pipeline, 31 Jest tests. |
| [Signals — Remove Brief Metadata](changes/dashboard-ui/) | 2026-03-29 | `d5803bf` | Removed internal brief metadata (model name + generated timestamp) from signals page UI. Audience-facing briefing should not expose `gpt-4o` model attribution or generation timestamps. |
| [Real-time Ticker Tape](changes/realtime-ticker-tape/) | 2026-03-31 | `3c15180`, `d6c02d8`, `7359d59` | Replaced static `futures-strip.js` + `macro-context.js` strips with TradingView embedded ticker tape widget (compact mode). Shared `js/lib/ticker-tape.js` injects after `<nav>` on `nav:ready` event — **all 6 pages** (market, watchlist, expected-moves, signals, events, autoresearch). 14 tickers: S&P (`FOREXCOM:SPXUSD`), Nasdaq (`FOREXCOM:NSXUSD`), Russell (`AMEX:IWM`), Dow (`AMEX:DIA`), Oil (`TVC:USOIL`), Gas (`CAPITALCOM:NATURALGAS`), Gold (`TVC:GOLD`), Silver (`TVC:SILVER`), Copper (`CAPITALCOM:COPPER`), Energy (`AMEX:XLE`), Dollar (`AMEX:UUP`), VIX (`TVC:VIX`), BTC (`BITSTAMP:BTCUSD`), ETH (`BITSTAMP:ETHUSD`). Real-time WebSocket for indices/commodities/crypto; 15-min delay for ETF proxies. Free tier — no API keys, zero maintenance. Old `macro-context.js` strip removed from market/watchlist/signals (was showing stale `prices.json` data labeled as live). `futures-strip.js` no longer loaded on any page. **Ticker symbol requirements:** Always use the native/canonical symbol for index-like instruments — `TVC:VIX` (not `AMEX:UVXY`), `TVC:DXY` or `AMEX:UUP` for Dollar. UVXY is a leveraged ETF proxy and shows a different price/scale than VIX. **Bug fixed 2026-04-06:** VIX was incorrectly set to `AMEX:UVXY` (showing ~48 UVXY price instead of VIX ~20). Fixed to `TVC:VIX`. |
| [Dynamic Pair Ratios Strip](changes/dynamic-pair-ratios/) | 2026-03-31 | `fd93ebd` | Replaced hardcoded pair ratio pills on signals page with data-driven SMA50 computation. 8 pairs: XLY/XLP (consumer), HYG/SPY (credit), IWM/SPY (breadth), XLV/SPY (defensive), XLE/SPY (energy), IWM/QQQ (value/growth), GLD/SPY (safe haven), TLT/SPY (bonds). Signal = ratio of current prices vs ratio of SMA50 values from watchlist.json. 1% threshold: above → ↗ up, below → ↘ down, within → → neutral. RSP replaced by IWM/SPY (RSP not in watchlist). |
| [SPA v2 Rewrite — Phases 1-3](changes/spa-v2/OPENSPEC.md) | 2026-03-31 | `f773dd0`..`41d84a6` | **Full SPA rewrite, promoted to root Apr 1.** Vanilla JS, zero framework, zero build step. 33 files, ~6,800 lines (vs v1's ~7,000 with duplication). v1 archived to `v1/`. Route `#airesearcher` (was `#autoresearch`, legacy redirect in place). **Phase 1:** Single `index.html` shell, hash router (`#market`, `#signals`, etc.), `preferences.js` (localStorage persistence), `shell.js` (nav, ticker tape toggle, hamburger), market page extracted. **Phase 2:** All 6 pages migrated — signals (13 setup cards, filters, right panel), watchlist (74-symbol table, sorting), expected moves (EM table, tier/filter tabs), events (countdowns, JSONL), AI Researcher (regime intelligence). Unified `detail-modal.js` (merged 3 v1 implementations). Shared `fear-greed.js` + `vix-regime.js` components. **Phase 3:** Lucide Icons (replaced ALL emoji + inline SVGs with consistent monoline SVGs via CDN), mobile responsive (hamburger nav, sticky columns, full-screen modals at ≤768px), collapsible sections (12 sections across 3 pages, state persists in preferences), loading skeletons (shimmer animation for all data areas). CSS token system (`variables.css` — single source for 40+ design tokens). |
| [Azure Static Web Apps](changes/spa-root OPENSPEC.md) | 2026-03-31 | `f9a12e8` | Deployed dashboard to Azure SWA (Free tier, eastus2, `rg-breakingtrades`). Auto-deploy via GitHub Actions on every push to main. URL: `brave-glacier-07c70460f.2.azurestaticapps.net`. PR preview environments enabled. Auth-ready (GitHub/Entra/Google login via config-only `staticwebapp.config.json`). Runs parallel to GitHub Pages — zero disruption. Subscription: `ME-MngEnvMCAP356394-idanshimon-1` ($0 cost — free tier). |
| [Quarterly EM History](changes/expected-moves/specs/quarterly-history.md) | 2026-04-03 | — | Backfilled 4 quarters (Q1–Q4 2025) of EM data from IB historical IV across 78 tickers. New `data/em-quarterly-history.json` (206KB). Visual range chart in EM detail modal: horizontal bars per quarter showing projected range vs actual outcome (green ✓ within / red ✗ outside). Per-ticker accuracy tracking. Overall 56.4% accuracy (124/220). |
| [Data Integrity Guards](changes/data-integrity-guards/OPENSPEC.md) | 2026-04-17 | `b3f4f1f`..`bd76ef2` | **Fix: Research page transition signal showed 0% above D200 when actual was +5.95%.** Three-tier fix: (1) `update-regime.py` now carries forward prior good values via new `assign_signal()` helper when compute hits `no_data`/`no_sma` sentinels — applied to all 15 signals, stamps `stale: True` flag for observability; (2) `update-prices.py` refuses to overwrite `prices.json` with 0 tickers, merges prior on <50% fetch with `source="yfinance+carryforward"`; (3) Added pytest suite — 15 tests in 0.79s covering `compute_sp_vs_d200` math + full `assign_signal` carry-forward matrix + `update_prices` write guards. |
| [Signals Page — Live Data + Badge Rethink](changes/signals-page-live-data/OPENSPEC.md) | 2026-04-17 | `21e76ef` | **Fix: Signals page showed stale/contradictory data** (e.g. MSFT "EXIT SIGNAL + BULL + Bear Flag" at $411.22). Removed hardcoded 13-entry `TICKERS` literal (~160 lines of mock data incl. fake sparklines, fake pattern/analysis strings, fake stop/entry/T1/T2 levels). Signals page now renders **entirely from `data/watchlist.json`** via new `mapWatchlistToSignal()` mapper. Canonical status enum (`exit \| triggered \| approaching \| active \| watching`) wired end-to-end. Replaced fake sparkline with **Technical Range bar** (52w low → 52w high + SMA20/50/200 markers, hover tooltips). Every badge + level pill + range marker now has a `title=` tooltip explaining the current value and why it's lit. Dropped redundant pattern + volatility badges (ATR% shown in stats row instead). Added RSI overbought/oversold + earnings-proximity contextual badges. Tests: `tests/signals.test.js` — 13 Jest tests incl. live-data integration guard against contradiction bugs. Docs: `docs/SIGNALS.md`. |
| [EM Staleness Guard + Anchor Fix](changes/expected-moves/) | 2026-04-28 | **Bug fix:** 75/85 tickers had stale Apr 10 weekly anchors (2.5 weeks old). Root cause: GH Actions + Mac cron ran at exact same time (4:20 PM ET) → rebase conflict → Friday `--tier all` commit lost. Fix: (1) Mac cron shifted to 4:25 PM (5-min offset, Mac wins push), (2) staleness guard in `eod-update.sh` — if anchor >7 days old, forces `--tier all` regardless of weekday, (3) one-time backfill injected Apr 24 closes into history for all 73 stale tickers. EM page now shows correct current-week bands. |
| [Autoresearch Summary Wired to EOD](changes/ci-data-pipeline/) | 2026-04-28 | `autoresearch/summarize.py` added as step 5b in `eod-update.sh`. Was orphaned (never auto-ran), causing AI Researcher page to go 11 days stale. Now regenerates every EOD from latest optimizer results. |
| [EM Tests — Staleness + Anchor](changes/expected-moves/) | 2026-04-28 | Added market-day-aware staleness test to `tests/expected-moves.test.js`: trading days allow 28h max age, weekends 72h. Fixed incorrect symmetry test (was checking `close` as midpoint; correct is `anchor_close`). Added per-ticker weekly expiry check (warns on stale tickers, doesn't fail for recently-updated ones). 17/17 tests pass. | v1: sticky `thead th { top: 0 }` pinned under the 48px nav (offset bug). v2: `em-pos-marker` (absolute, `top: -3px`) painted above the sticky thead on scroll because `.em-table-wrap` had no stacking context scope. Proper fix: `isolation: isolate` on `.em-table-wrap` + `tbody tr { position: relative; z-index: 0 }` for row-level containment + v1 offset correction. Codified in `docs/DESIGN_SYSTEM.md` as the canonical sticky-header pattern. CSS-only, applies to v1 and v2. |
| [EM History Sparkline + Triage UX](changes/em-history-sparkline/OPENSPEC.md) | 2026-05-09 | `a8bfb02` | **v1 EM page improvements (PR #3).** Surfaces previously-unused producer data + tightens triage UX. (1) Detail-modal history chart — inline SVG of close-price line + shaded predicted weekly EM band over last 30 days, with breach-day markers (red up / cyan down). Uses the existing `history[]` array (up to 33 daily snapshots/ticker). (2) **IV Trend column replaces the redundant Risk Meter column** — computes avg weekly EM% over last 5 history snapshots vs prior 5; displays ▲/▼/— with delta %, sortable. (3) Bias-aware Buy Zone / Extended stats — `8 (3 BULL · 5 MIXED)` instead of flat count. (4) Alert-tag filter chips (🟢 Buy zone / 🔴 Extended / ⚡ Outside EM) — multi-toggle, OR-within, AND with category filter. (5) **Default sort = position ASC** when no user sort active — buy zones float to top (was JSON insertion order). (6) Stale-data top-of-page banner when EM data >48h old (Mar 27 incident showed timestamp alone was easy to miss). Single-file change to `v1/expected-moves.html` + new `tests/em-page-improvements.test.js` (22 Jest tests covering IV trend, history-chart geometry, breach detection, bias breakdown, default sort, alert-filter predicate, real-data smoke). Test counts: jest 137→159 all green; `test-em-formula.js` unchanged. No producer changes, no new dependencies. v2 SPA parity tracked as follow-up. |
| [Economic Calendar — US 3-Star First Slice](changes/event-calendar/) | 2026-05-10 | (this PR) | `scripts/fetch-economic-calendar.py` pulls US 3-star events from Investing.com's public AJAX endpoint (no API key, `country[]=5&importance[]=3&timeZone=55`). Top 3 upcoming events within 14 days written to `data/economic-calendar.json`. Wired into `eod-update.sh` step 5c (non-fatal). `events.js` merges them into Live Countdowns with forecast/previous inline (e.g. `Core CPI (MoM) — fc 0.3% / prev 0.2%`). Also fixed stale-events bugs: defense-in-depth filters in events.js (Live Countdowns require `deadline > now`; Upcoming-7-days requires real future deadline; mini strip same), tz-bug fix in `bt-event expire` (now handles naive datetimes, date-only `2026-04-08`, range `A/B`). Synced `data/events.jsonl` from parent (was 4 lines from Apr 2; parent had 146; auto-expired 34 past-deadline events). Follow-ups: hook FRED/FOMC scrapes, severity tiering. |
| [Watchlist EM Columns + Staleness Banner](changes/em-watchlist-columns/) | 2026-05-10 | (this PR) | New columns on the watchlist table: **Weekly EM %** (color-graded: high vol >3.5% orange, compressed <1.5% dim, mid bright) + **EM Pos** (position-in-range bar with BUY/MID/EXT label, breach-aware below 0% / above 100%, hoverable tooltip with lower/upper $). Both default-on, sortable, toggleable via the column menu (persisted to localStorage). Synthetic fields `emPct` / `emPos` computed once at load from `data/expected-moves.json`'s existing `weekly` tier. Plus **EM staleness banner** above the stat bar when data is >24h old on weekdays / >72h on weekends — surfaces the Mar 27 anchor-stale problem class without users having to spot a red timestamp. Pure frontend, no producer changes. |
| [Earnings Days — Live Recompute](changes/earnings-days-live/) | 2026-05-10 | (this PR) | **Fix: snapshot's `earningsDays` was stale by definition.** Producer (`export-yfinance-fallback.py`) precomputes `(earnings_date − today)` once at export time and bakes it into `watchlist.json` as `earningsDays`. After that, every page render shows the same integer until the next export — so NVDA at $215 showed "Earnings 12d" on May 10 when it was actually 10 days away (snapshot was from May 8). Fix derives `earningsDays` live from the canonical `earningsDate` string on every page load. Applied in `watchlist.js` (`_refreshEarningsDays(list)` after `loadData`) and `signals.js` (`_liveEarningsDays()` inside `mapWatchlistToSignal()`). Window: -7 days (recently past, still surfaced as negative for context) to +365 days (else nulled out). Tests: `watchlist.test.js` +8 cases covering positive/zero/recent-past/far-past/far-future/no-date/bad-date. 159 → 167 jest tests green. |
| [EOD Workflow Push Bug — eod-update.sh owns commit+push](changes/ci-data-pipeline/) | 2026-05-31 | `b63e99f` | **Fix: Friday EM(all) commits stranded on the runner for weeks.** `.github/workflows/eod-update.yml` set `BT_SKIP_PUSH=1`, then ran a downstream "Commit and push" step that staged `data/` and committed only on a non-empty staged diff. But `eod-update.sh` had already committed in-runner (with `BreakingTrades Bot` identity), so `git diff --staged --quiet` always returned true and the downstream step printed `No changes to commit` and exited 0. Net effect: every Friday's `EM(all)` commit was created on the ephemeral runner and discarded — origin only ever received intraday `EM(daily)` commits (9 ETFs). Symptom on origin: 77/86 tickers stuck on 2026-05-15/20 timestamps despite 5 consecutive `success` runs. Fix: removed `BT_SKIP_PUSH=1` and the redundant downstream commit step from `eod-update.yml`; `eod-update.sh` now owns commit+push end-to-end via its existing rebase + ours/theirs conflict resolution (vs F&G concurrent pusher) + 3-retry path. Workflow step now only sets git identity and runs the script. Diagnostic: `failure_count` in EM JSON lies when the issue is push-side rather than fetch-side — the **timestamp histogram** is the real health check (one fresh cluster on Friday's date + 9 fresh ETFs on today). Shipped: manual `--tier all` refresh of stranded data (82/85, 3 expected non-equity errors: BTC-USD, CL=F, STO.AX) + workflow patch. Follow-ups: harden stale-patrol to compare against an expected-trading-day baseline (catches silent yfinance failures *during* step 4, not just tickers omitted from the run); clean up duplicate `BRK B` (space) entry — `BRK-B` is canonical. |
| [Events Page — Earnings Injection](changes/events-page-earnings/) | 2026-05-10 | (this PR) | **Fix: Events page Earnings tab was empty.** Page only knew about `events.jsonl`, `economic-calendar.json`, and `market-hours.json`; it never read `watchlist.json`'s `earningsDate` field. Added a 4th fetch in `loadEvents()` that injects every watchlist ticker with an upcoming `earningsDate` (next 30 days) as a synthetic event: `category=earnings`, `severity=critical` for mega caps within 7d (NVDA/AAPL/MSFT/META/GOOGL/AMZN/TSLA/SPY/QQQ) else `high`/`medium` by proximity, `countdown=true` when ≤14d, deadline at 4 PM ET (post-close, conservative default for AMC reports). Days-until is recomputed live every render so the badge tracks the calendar even between EOD exports. Also adds a missing comment label above the NYSE-holiday block. No producer changes. Live Countdowns now shows NVDA / TGT / ZM / BILI / BJ / NBIS earnings alongside econ events; Earnings filter tab functional. |
| [Autoresearch Durable Run Ledger](changes/autoresearch-run-ledger/proposal.md) | 2026-05-31 | `8ee2db2` (local) | **Fix: autoresearch cross-run memory was destroyed every run.** `runner.sh` wrote `data/autoresearch-results.json` with a hardcoded `'history': []`, clobbering the prior run's record each invocation — the only durable trace was fragile git commit messages, and the all-time-peak May-22 run (0.6673) lost its winning config entirely. Added an append-only ledger at `data/autoresearch-history.json` (top keys `_schema/_note/_source/last_updated/runs`; per-run `date/timestamp/best_score/baseline_score/best_commit/best_config/experiments/improved_count/improved_commits/source`). Backfilled **10 runs** (2026-03-29 → 2026-05-31) by clustering git commits (same day + ≤1hr gap) + the orphaned May-22 `history.json` entry; pre-2026-04-24 scores flagged not-comparable (earlier evaluator/ticker universe). Patched `runner.sh` to read→append→stamp `last_updated`→write (tolerates legacy bare-list shape, best-effort non-fatal). `results.json` stays last-run-only; ledger is the durable cross-run record + agent anomaly-detection surface. Verified on the 2026-05-31 run (baseline 0.6597 → best 0.6627, ema_short 8→10). **Known gap (follow-up):** ledger stores composite/baseline only, not per-regime `regime_scores` — composite is confounded by the April evaluator change, so the real market signal lives in the per-regime breakdown; snapshotting `best_config.regime_scores` would make it a true regime monitor. `summarize.py` not yet wired to surface the ledger on `#airesearcher`. Local commit, **not pushed** (rule 6). |

| [Watchlist — Space Category + 6 Tickers](changes/watchlist-space-category/) | 2026-05-31 | (local) | **New "Space" sector/group added to the watchlist universe.** Added 6 liquid pure-play space tickers to the master `WATCHLIST` in `scripts/export-dashboard-data.py` (the universe source of truth; `data/watchlist.json` is generated output): FLY (Firefly Aerospace), RKLB (Rocket Lab), ASTS (AST SpaceMobile), LUNR (Intuitive Machines), RDW (Redwire), PL (Planet Labs) — all `sector:'Space', group:'Space'`. yfinance auto-fetches metrics on export; frontend (`js/pages/watchlist.js`) buckets sector/group filters dynamically so the new category surfaces with no UI change. Mapped `Space → Industrials` in both `SECTOR_NAME_MAP` dicts (`export-sector-rotation.py` ~L143, `export-yfinance-fallback.py` ~L382) so the tickers roll into the RRG/sector-rotation risk overlay (no GICS "Space" sector exists). Verified export: all 6 populated with price/SMA20/bias (FLY $46.49, RKLB $143.48, ASTS $113.41, LUNR $43.83, RDW $24.57, PL $51.14 — all bull). Left out KTOS/BKSY/RCAT (defense-adjacent) and ARKX/UFO (ETFs) — verified live, easy to add later. Local commit, not pushed (rule 6). |

| [Empirical-Priors Dashboard — `/#autoresearch` Layer](changes/archive/2026-06-06-autoresearch-empirical-priors-ui/) | 2026-06-06 | `3778a1f` (dashboard), `c00d36e` (parent submodule bump) | **Three new dashboard sections on the `/#autoresearch` SPA route surfacing the parent repo's empirical-priors layer.** Spec: `changes/archive/2026-06-06-autoresearch-empirical-priors-ui/` (10 ADDED requirements, capability `research-page-empirical-priors-ui`). Parent-repo data layer spec: `<parent>/openspec/changes/archive/2026-06-06-autoresearch-empirical-priors/`. Three sections: (1) **Empirical Priors** — searchable study table (ticker / condition / N / 1Y hit rate / 1Y mean / linked rules), click-to-detail modal with forward-return table by horizon (1d/5d/20d/60d/252d) + regime breakdown + notes; (2) **Regime Explorer** — per-regime cards (date range, member days, per-ticker cumulative return, studies touching that regime); (3) **Rule Lineage** — rule table with evidence badges, click-to-detail evidence chain (study citations color-coded by weight: primary green / contradictory red / supporting muted; live observations orange). `scripts/export-dashboard-data.py` adds `export_empirical_priors_layer()` reading `<parent>/data/empirical-priors.jsonl` + `<parent>/data/trading-days.parquet` + `<parent>/agents/tom-fxevolution/RULES.json`, emitting `data/{empirical-priors,regime-explorer,rule-lineage}.json`. `js/pages/autoresearch.js` renders the 3 sections OUTSIDE the regimeData guard so they work standalone (with fallback collapsibles registration). **Routing gotcha hit and documented:** `v1/autoresearch.html` is a STANDALONE legacy page that ignores `js/pages/autoresearch.js` entirely — edits only show up at `/#autoresearch` SPA route. Day-one finding: the layer surfaced that R504-style "fade panic on QQQ -5%" claims overstate hit rate — full-history 252d hit rate is 35% (n=57), the higher numbers only hold in non-bear regimes. Caveat: `rule-evidence-map.json` curation was sloppy on the seed batch (R504 mismapped to QQQ-drop studies); discipline note added to docs. **Note:** proposal was filed retroactively (after `3778a1f` shipped) — see proposal.md for the rule-7 discipline note. |
| [Breadth Gauge — Remove Rounded-Cap Blob Artifacts](changes/market-breadth/) | 2026-06-06 | `b34539b` (dashboard) | **Pure visual fix.** The active-arc on the Market Breadth gauge in `js/pages/market.js:349` used `stroke-linecap="round"` with `stroke-width=18`, producing visible orange blob protrusions at both ends of the colored arc (one at 0%, one at the current value). Looked like glitched UI markers rather than caps. One-character fix: `round` → `butt`. Active arc now ends cleanly at both edges; needle still indicates the precise value. No openspec proposal needed (pure bug fix per dashboard rules). |
| [Week Ahead — Weekly Catalyst Scan + Tom Narrative + IPO Regime Tracker](changes/weekly-catalyst-brief/) | 2026-06-09 | PR #4 (`d00183e`..`40ab0d3`) | **New `#week-ahead` SPA route + Sunday-evening data pipeline.** Surfaces every catalyst class for the upcoming trading week into one normalized snapshot, runs it through Tom's persona for a forward-looking weekly brief, and tracks recent IPO post-debut performance as a regime signal for upcoming IPOs. Producers (4 new scripts): `fetch-earnings-calendar.py` (Investing.com US earnings, market-cap+BMO/AMC, 3-attempt cloudflare retry), `fetch-fomc-calendar.py` (federalreserve.gov FOMC schedule + recent speeches), `fetch-economic-calendar.py` (extended w/ `--week-mode`/`--dry-run`/retry), `track-ipo-performance.py` (yfinance post-debut metrics — day-1 high/close, return_from_ipo, broke_ipo_price flag, status_tag, regime aggregation: robust/mixed/exhaustion). Orchestrator `weekly-catalyst-scan.py` runs all 4, reads events.jsonl + EM data, normalizes 5 categories (macro/fed/earnings/ipo/geopolitical) into `data/weekly-catalysts.json` with hottest-day scoring + per-day breakdown. Tom narrative `generate-week-ahead-brief.py` (Sunday-night cadence, schema: headline+thesis+hot_days+rule_triggers+what_im_watching) → `data/week-ahead.json`. Frontend: Tom brief hero, hot-days strip with intensity bars, 4-column catalyst grid with countdown timers, Index/sector EM cards with breach annotations, Recent IPO Performance section (regime banner + per-IPO cards with status tags + day-1 high / now / from-d1-high stats + BROKE badge + volume collapse signal). `data/ipo-history.jsonl` seeded with 7 recent 2026 IPOs (QNT/CBRS/HAWK/AVEX/ARXS/PSUS/MMED). Bundled fixes: parent `scripts/bt-event` had same 60-char title-truncation bug as `extract-events.py` — patched with `_clip_title` (110ch, word-boundary-aware). QNT IPO regime-warning event added to events.jsonl. **Also shipped** signals-page UX upgrade: ticker-specific structured reasoning behind status badges (Triggered/Approaching/Active/Exit/Watching) with hover tooltip + click "Why?" popover surfacing the conditions that fired (price-vs-MA %, golden/death cross, SMA200 caveat for daily-only reclaims, RSI/ATR/52w-high context, earnings-proximity warning). |


## Active Changes (in progress)

| Change | Status | Description |
|--------|--------|-------------|
| [SPA v2 Cutover](changes/spa-root OPENSPEC.md) | Phase 4 pending | Move `root ` to root, archive v1, configure auth, custom domain |
| [Data Pipeline](changes/data-pipeline/) | Partial | Core signals + EOD pipeline shipped; lifecycle classifier + per-ticker Tom pending |

## Planned Changes (not started)

| Change | Priority | Description |
|--------|----------|-------------|
| Economic Calendar Integration | **High** | ✅ **First slice shipped 2026-05-10** (see Shipped Changes). Follow-ups: integrate FRED releases, FOMC schedule (vs. just bullish-icon scoring), and FOMC speakers; add severity tiering by event class (CPI > existing-home-sales). |
| **Signal Scanner** | **Critical** | Nightly scanner: EMA 13/26 crossover state × weekly alignment × regime filter × EM position × volume profile S/R. Outputs `data/scan-results.json` + Telegram alerts. Replaces static signals cards with computed, ranked setups. Runs in EOD pipeline. |
| Autoresearch Ledger — Regime Snapshot | **Medium** | Follow-up to the durable run ledger (shipped 2026-05-31). Extend `runner.sh`'s ledger append to snapshot `best_config`'s per-regime `regime_scores` (+ None / trade-count flags) per run. Composite is confounded by the April evaluator change, so the real market signal lives in the per-regime breakdown — capturing it turns the ledger from a config-drift log into a true market-regime monitor / anomaly detector. Then wire `summarize.py` to surface regime drift on `#airesearcher`. |
| **GEX / Gamma Levels (Self-Computed)** | **High** | Compute dealer gamma exposure from options chain data (IB Gateway + yfinance). Put wall = support, call wall = resistance. Net GEX sign (positive = mean-reversion, negative = trend-amplifying). Highest Volume Level (HVL) as price magnet. Output: `data/gex.json` per major ticker. No paid service — we have the options data from IB. |
| **Volume Profile S/R Levels** | **High** | Compute support/resistance from volume-at-price (VAP) using IB 1h/daily bars. Point of Control (POC), Value Area High/Low (VAH/VAL), High Volume Nodes (HVN). Display on detail modal charts + feed into signal scanner as entry/exit levels. Tom's primary S/R method. |
| **Dark Pool Data Integration** | **Medium** | Volume Leaders (~$50/mo) or similar dark pool feed. Institutional block trade data — dollar amounts + price levels reveal smart money positioning. Feed into signal scanner as confirmation layer. Evaluate: free alternatives (SEC 13F delayed, FINRA ADF/TRF public data). |
| **Backtest Engine** | **High** | Backtest EMA 13/26 system across 222-ticker history. Measure: win rate, avg R, max drawdown, Sharpe. Test regime overlay + EM filter impact. Output: `backtest/results.json` + equity curve. Must answer: does this system have a quantitative edge? |
| **Trade Alerts → Telegram** | **High** | Wire scanner + EM breach + regime shift alerts to BreakingTrades Telegram bot. "TSLA below EM range (buy zone)", "NVDA: EMA 13×26 bullish cross on daily", "Regime → FEAR". Runs in EOD pipeline + intraday price refresh. |
| **Position Sizing Calculator** | **Medium** | In detail modal: account size × risk% × (entry - stop) = shares. Stop defaults to EM lower. R-multiple targets at 1R, 2R, 3R. |
| **Trade Journal** | **Medium** | Log entries/exits with thesis, R-multiple, chart screenshot. Track which rules/setups actually work for YOUR trading. |
| SPA v2 Cutover (Phase 4) | **Medium** | Retire v1, configure Azure SWA auth + custom domain. v2 SPA is production. |
| [Regime Intelligence Dashboard](changes/regime-intelligence/OPENSPEC.md) | — | ✅ **Shipped** as AI Researcher (`#airesearcher`). Computed regime score from 15 weighted signals, 7 regimes (CRISIS→EUPHORIA), Tom's rules mapped, playbook per regime, transition signals, market internals, commodity chain, regime history. Added to EOD pipeline. *(Listed here only to retain the cross-link from older docs — see Shipped Changes for the canonical entry.)* |
| Earnings Calendar | High | Dedicated earnings tool — EarningsWhisper (primary), TradingView (secondary). Upcoming earnings for watchlist + broader market. |
| EM History Sparkline | — | ✅ **Shipped 2026-05-09 (`a8bfb02`)** — see Shipped Changes → "EM History Sparkline + Triage UX". |
| EM Columns on Watchlist | — | ✅ **Shipped 2026-05-10** — see Shipped Changes → "Watchlist EM Columns + Staleness Banner". |
| [AI-Ready Architecture](changes/ai-ready-architecture/) | Medium | Tom agent multi-channel output: alerts, ticker notes, market pulse, feed panel |
| Trade Lifecycle Engine | Medium | 9-state status classifier for ticker cards |
| Per-Ticker Tom's Take | Low | Pre-generated LLM analysis per symbol |
| [Tom Chat Widget](changes/tom-chat/) | Low | Interactive chat — deferred to Phase 3 of AI-Ready Architecture |
| Event Calendar — Phase 2 | Medium | FastAPI route in market-watcher; external calendar API polling (FRED releases, macro data) |
| Economic Calendar Integration | **High** | Auto-populate Events page with economic calendar data (FOMC, CPI, PPI, NFP, GDP, jobless claims, FRED releases). Source: Investing.com economic calendar API or TradingView economic calendar widget. Show upcoming releases with consensus/prior, countdown timers, and severity based on market impact. Should merge seamlessly with existing JSONL events + NYSE holiday auto-injection. |

## Archived

| Change | Date | Description |
|--------|------|-------------|
| [Watchlist Engine](changes/archive/2026-03-17-watchlist-engine/) | 2026-03-17 | Initial watchlist data + export |
| [Dashboard Frontend](changes/archive/2026-03-17-dashboard-frontend/) | 2026-03-17 | Initial 3-page dashboard build |
| [Tom Agent](changes/archive/2026-03-17-tom-agent/) | 2026-03-17 | Tom persona files + rules |
